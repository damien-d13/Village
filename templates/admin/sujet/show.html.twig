{% extends 'base.html.twig' %}

{% block title %}Sujet{% endblock %}

{% block body %}
    <div class="container">
        <h1>Sujet</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sujet.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ sujet.label }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('admin_sujet_index') }}">back to list</a>

        <a href="{{ path('admin_sujet_edit', {'id': sujet.id}) }}">edit</a>

        {{ include('admin/sujet/_delete_form.html.twig') }}
    </div>
{% endblock %}
