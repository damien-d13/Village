{% extends 'base.html.twig' %}

{% block title %}Information{% endblock %}

{% block body %}
    <div class="container">
        <h1>Information</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ information.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ information.title }}</td>
            </tr>
            <tr>
                <th>Subtitle</th>
                <td>{{ information.subtitle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ information.description }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('admin_information_index') }}">back to list</a>

        <a href="{{ path('admin_information_edit', {'id': information.id}) }}">edit</a>

        {{ include('admin/information/_delete_form.html.twig') }}
    </div>
{% endblock %}
